<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Liste des produits publiés</title>
</head>
<body>

    <h1>Produits publiés</h1>

    <ul>
        {% for product in products %}
            <li>{{ product.title }}</li> {# Affiche uniquement le titre de chaque produit #}
        {% else %}
            <li>Aucun produit publié.</li> {# Message si aucun produit n'est trouvé #}
        {% endfor %}
    </ul>

</body>
</html>