<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Détail de la catégorie</title>
</head>
<body>

    <h1>{{ category.title }}</h1>

    <p>Description : {{ category.description }}</p>

    <p>Créée le : {{ category.createdAt|date('d/m/Y H:i') }}</p>
    <p>Modifiée le : {{ category.updatedAt|date('d/m/Y H:i') }}</p>

</body